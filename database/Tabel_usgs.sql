CREATE TABLE earthquake_2025 (
    id VARCHAR(50),
    magnitude FLOAT,
    place VARCHAR(500),
    event_time DATETIME2,
    updated BIGINT,
    longitude FLOAT,
    latitude FLOAT,
    depth FLOAT,
    year INT,
    month INT,
    event_date DATE,
    kategori VARCHAR(50)
);